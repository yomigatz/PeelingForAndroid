<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yomigatz.peeling">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DayTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".colorful.ColorFulActivity"/>

        <activity android:name=".colorful.RecyclerActivity"/>
        
        <activity android:name=".peeling.activity.Activity1"/>

        <activity android:name=".peeling.activity.Activity2"/>
        <activity android:name=".peeling.activity.Activity3"/>
    </application>

</manifest>